import { Fragment } from "react";


interface CameraScreenRecorderProps {
    // props component
}

export default function CameraScreenRecorder({} : CameraScreenRecorderProps) {

    return (
        <Fragment>

        </Fragment>
    )
}